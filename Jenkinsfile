pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'sudo docker images'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
